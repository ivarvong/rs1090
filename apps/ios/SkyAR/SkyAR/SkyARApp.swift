// SkyARApp.swift — app entry. Replaces the file Xcode auto-creates;
// the only difference vs. the default template is that the root
// view is ours.

import SwiftUI

@main
struct SkyARApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
